# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (Supabase PostgreSQL - Settings → Database → Connection string, Transaction mode)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:6543/postgres

# Auth (Better Auth)
BETTER_AUTH_SECRET=  # Run: openssl rand -base64 32

# Spotify OAuth (same credentials used in backend)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
